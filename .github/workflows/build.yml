name: Build PS4 Library

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download project
        uses: actions/checkout@v4

      - name: Show project files
        run: |
          echo "Pisscou PS4 Library"
          ls -la
